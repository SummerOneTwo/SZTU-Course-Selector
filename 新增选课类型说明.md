# 新增选课类型抓取说明

## 更新内容

`crawler.py` 现已支持抓取以下 7 种选课类型的课程数据：

| 序号 | 选课类型 | 接口标识 | 说明 |
|------|---------|---------|------|
| 1 | 本学期计划选课 | `Bxqjhxk` | 专业培养方案内课程 |
| 2 | 跨年级选课 | `Knjxk` | 跨年级选修课程 |
| 3 | 公选课选课 | `Ggxxkxk` | 全校通识选修课 |
| 4 | 实验选课 | `Syxk` | 实验类课程 |
| 5 | **专业内跨年级选课** | `Zynknjxk` ⭐新增 | 同专业内跨年级选课 |
| 6 | **跨专业选课** | `Kzyxkkc` ⭐新增 | 跨专业选修课程 |

## 使用方法

### 1. 基本运行

```bash
uv run crawler.py
```

脚本会自动抓取所有 7 种选课类型的课程数据，并保存到 `选课数据.json`。

### 2. 配置文件要求

确保 `config.toml` 中已正确填写批次 ID：

```toml
[settings]
jx0502zbid = "你的批次ID"
```

## 关于搜索功能

### 当前实现

新增的专业内跨年级选课和跨专业选课接口已配置基础查询参数：

```
?kcxx=&skls=&skxq=&skjc=&sfym=false&sfct=true&sfxx=true&skfs=
```

参数说明：
- `kcxx`: 课程名称/关键词搜索
- `skls`: 授课老师
- `skxq`: 上课星期（1-7）
- `skjc`: 上课节次
- `sfym`: 是否已满
- `sfct`: 是否冲突
- `sfxx`: 是否限选
- `skfs`: 上课方式

### 使用搜索功能

如果需要搜索特定课程，可以修改代码中的 URL 参数：

```python
# 示例：搜索包含"数学"的课程
url_zynk = "https://jwxt.sztu.edu.cn/jsxsd/xsxkkc/xsxkZynknjxk?kcxx=数学&skls=&skxq=&skjc=&sfym=false&sfct=true&sfxx=true&skfs="
```

### 高级搜索功能（可选扩展）

如果需要更灵活的搜索，可以考虑实现以下功能：

1. **关键词搜索**: 支持课程名、教师名模糊匹配
2. **时间筛选**: 按星期、节次筛选
3. **容量筛选**: 只显示有剩余名额的课程
4. **本地缓存**: 抓取全部数据后本地搜索，减少服务器请求

## 注意事项

### 1. 接口地址验证

新增的两个接口地址是基于教务系统命名规则推测的：
- `xsxkZynknjxk` - 专业内跨年级选课
- `xsxkKzyxkkc` - 跨专业选课

如果运行时返回错误或没有数据，可能需要通过浏览器抓包获取准确的接口地址。

### 2. 搜索限制

某些选课类型可能需要先进行搜索才会显示课程数据。如果直接请求返回空数据，可以尝试：
1. 添加搜索关键词（`kcxx` 参数）
2. 检查是否需要额外的 POST 参数
3. 通过浏览器手动操作并抓包分析

### 3. 数据合并

所有抓取到的课程数据会合并保存到同一个 `选课数据.json` 文件中。建议定期检查数据完整性，避免重复或遗漏。

## 故障排除

### 问题：新增接口返回空数据

**可能原因：**
1. 接口地址不正确
2. 该选课类型在当前批次未开放
3. 需要先搜索才能显示数据

**解决方法：**
1. 通过浏览器 F12 → Network 抓包确认准确接口地址
2. 检查选课批次 ID 是否正确
3. 尝试添加搜索关键词参数

### 问题：搜索功能不生效

**可能原因：**
1. 搜索参数格式不正确
2. 教务系统需要特定的编码格式
3. 搜索接口需要 POST 而非 GET

**解决方法：**
1. 检查 URL 编码（特别是中文关键词）
2. 通过浏览器抓包查看实际的请求方式和参数
3. 参考已有的 `fetch_courses_from_url` 函数实现

## 后续优化建议

1. **接口自动发现**: 实现自动扫描教务系统接口，减少手动配置
2. **智能重试**: 针对不同的错误类型实现不同的重试策略
3. **数据增量更新**: 只抓取新增或变更的课程数据，提高效率
4. **可视化界面**: 开发简单的 Web 界面，方便非技术用户使用

---

如有问题或需要进一步的帮助，请随时反馈！
